# /etc/systemd/system/docker.service.d/wait-for-hdd.conf
#
# Don't start docker (and nextcloud) before the external disk is mounted.
# The disk is "nofail" in fstab so the pi still boots without it, but then
# docker would start with an empty /mnt/hdd.
[Unit]
RequiresMountsFor=/mnt/hdd
